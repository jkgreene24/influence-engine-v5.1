import React from 'react';
import ReactDOM from 'react-dom';
import InfluenceEngine from './App';

ReactDOM.render(
  <React.StrictMode>
    <InfluenceEngine />
  </React.StrictMode>,
  document.getElementById('root')
);
